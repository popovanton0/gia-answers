package com.popov.egeanswers.dao

import androidx.lifecycle.LiveData
import com.popov.egeanswers.model.LarinOGEVariant
import com.popov.egeanswers.model.LarinVariant
import io.realm.Realm

/*
abstract class LarinVariantDao {
    protected val realm = Realm.getDefaultInstance()

    abstract suspend fun getAll(): List<LarinVariant>
    abstract fun getAllLiveData(): LiveData<List<LarinVariant>>
    abstract fun getVariant(number: Int): LarinVariant
    abstract suspend fun deleteVariant(number: Int): Boolean
    abstract fun deleteAll(): Boolean
    fun close() = realm.close()
}*/
